# App runtime environment: prod | dev | test
APP_ENV=dev

# Dev convenience: 1 enables autologin behavior (only honored when APP_ENV=dev)
APP_AUTOLOGIN=0

# Optional dev password used for autologin (leave empty in prod/test)
APP_DEV_PASSWORD=
